<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="sv.edu.udb.tallerdos">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icpprincipal"
        android:label="TallerDos"
        android:roundIcon="@drawable/icpprincipal"
        android:supportsRtl="true"
        android:theme="@style/Theme.TallerDos">
        <activity android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity" android:exported="false"/>
    </application>

</manifest>